[project]
name = "csrgenerator"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "flask",
    "cryptography",
]

[dependency-groups]
dev = [
    "pytest",
    "flake8",
]

[tool.pytest.ini_options]
testpaths = ["tests.py"]
